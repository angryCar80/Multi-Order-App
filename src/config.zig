const std = @import("std");

// THIS IS JUST TODO
// The Main Config Look
const Config = struct {
    ui_theme: []const u8,
    ui_status_bar: bool,
    shell_history: bool,
    todo_autosave: bool,
};
